const { makeStyles } = require("@material-ui/core");

const AddEventStyle = makeStyles({
  Container: {
    padding: "40px",
  },
  inputField: {
    marginBottom: "20px",
  },

  Heading: {
    textAlign: "center",
    marginBottom: "40px",
  },
});

export default AddEventStyle;
